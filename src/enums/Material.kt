package enums

// Enum com os materiais possiveis de uma caixa d'agua.
// Enum restringe o campo a valores predefinidos, evitando erros de digitacao no banco.
enum class Material {
    POLIETILENO,
    FIBRA_DE_VIDRO,
    INOX,
}
