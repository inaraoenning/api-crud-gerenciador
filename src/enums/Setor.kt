package enums

// Enum que define os setores da empresa.
// Usamos enum para limitar as opcoes e evitar que o usuario digite um setor invalido.
enum class Setor {
    FINANCEIRO,
    COMERCIAL,
    MANUTENCAO,
}
